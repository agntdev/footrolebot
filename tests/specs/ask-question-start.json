[
  {
    "name": "Ask a question",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Begin a free-text question flow. Bot uses ForceReply to capture the user's question."
            }
          }
        ],
        "send": {
          "callback": "ask_question:start"
        }
      }
    ]
  }
]
