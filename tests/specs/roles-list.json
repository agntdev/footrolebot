[
  {
    "name": "Browse roles",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Show seeded list of ~8 common positions as inline keyboard; tap a role to view explanation."
            }
          }
        ],
        "send": {
          "callback": "roles:list"
        }
      }
    ]
  }
]
